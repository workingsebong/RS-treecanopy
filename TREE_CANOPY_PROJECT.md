# 항공영상 기반 Tree Canopy Instance Segmentation 프로젝트 초안

## 프로젝트 개요

이 프로젝트의 목표는 여름철 원격탐사 영상을 활용해 나무를 탐지하고, 최종적으로 **개별 tree canopy 지도**를 생성하는 것이다.

초기 아이디어는 `object detection`으로 나무 위치를 찾은 뒤 `NDVI`를 이용해 canopy 크기를 추정하는 방식이었으나, 실제 구현 관점에서는 **항공영상 기반 instance segmentation**이 더 직접적이고 정확하다.

---

## 목표

- 여름철 항공영상 또는 정사영상을 이용해 개별 나무를 식별한다.
- 각 나무의 canopy를 개별 `mask` 단위로 추출한다.
- 나무 위치, 개수, canopy 면적을 포함하는 tree map을 생성한다.
- 필요 시 결과를 `GeoJSON`, `Shapefile`, `GeoPackage` 등 GIS 형식으로 저장한다.

---

## 왜 Landsat만으로는 어려운가

`Landsat` 영상은 보통 공간해상도가 `30 m` 수준이기 때문에, 개별 나무 위치나 canopy 크기를 정확히 추정하기에는 한계가 크다.

따라서 Landsat는 다음 목적에는 적합하다.

- 산림/수목 피복 지역 구분
- 넓은 면적의 vegetation cover 분석
- 시기별 식생 변화 추적

하지만 다음 목적에는 적합하지 않다.

- 개별 나무 단위 탐지
- 개별 canopy 경계 추출
- 정밀한 canopy 면적 계산

즉, **개별 tree map**이 목표라면 항공영상 또는 초고해상도 위성영상이 필요하다.

---

## 권장 데이터

### 1. 영상 조건

- 계절: 여름철 `leaf-on` 시기
- 영상 유형: 항공 정사영상 또는 드론 정사영상
- 권장 해상도: `10-30 cm/pixel`
- 가능하면 `RGB`보다 `RGB + NIR` 밴드 구성이 더 유리함

### 2. 권장 이유

- 개별 나무 crown 경계가 육안으로 식별 가능함
- crown 간 경계 분리가 상대적으로 쉬움
- canopy 면적 계산이 가능함

---

## 제안 방법론

### 1. 데이터 준비

- 대상 지역의 여름철 항공영상 확보
- 좌표계와 해상도 확인
- 대형 영상을 `512 x 512` 또는 `1024 x 1024` 타일로 분할
- 학습/검증/테스트 셋은 무작위가 아니라 **공간적으로 분리된 영역 기준**으로 나눔

### 2. 라벨링

라벨링 대상은 나무 중심점이 아니라 **개별 tree crown polygon**이다.

사용 가능한 도구:

- `CVAT`
- `Labelme`
- `Roboflow`
- `QGIS`

라벨 기준:

- 줄기 위치가 아니라 수관의 외곽 경계를 기준으로 그림
- 서로 겹치는 crown은 가능한 한 개별 객체로 분리
- 관목, 잔디, 그림자 영역은 제외

### 3. 모델링

우선순위는 아래와 같다.

#### 기본 추천

- `YOLO-seg`
  - 구현이 빠르고 베이스라인 구축이 쉬움
  - 실험 시작 단계에 적합

- `Mask R-CNN`
  - 전통적인 instance segmentation 모델
  - 결과 해석과 문헌 비교가 쉬움

#### 대안

- `Semantic Segmentation + Watershed`
  - 나무 crown이 서로 많이 붙어 있을 때 후보가 될 수 있음
  - 단독 모델보다 후처리 설계가 중요함

### 4. 식생지수 활용

`NDVI`는 canopy 경계를 직접 만드는 핵심 도구라기보다, **식생 여부를 보조적으로 판단하는 feature**로 사용하는 것이 적절하다.

즉, 추천 흐름은 다음과 같다.

- 주 모델이 개별 canopy `mask`를 예측
- `NDVI` 또는 `NIR` 기반 지표는 비식생 오탐 제거에 활용
- 그림자, 건물 가장자리, 잔디 오탐을 후처리로 보정

### 5. 후처리

- 너무 작은 객체 제거
- 비정상적으로 가는 mask 제거
- 인접 canopy 분리 보정
- 필요 시 GIS 벡터화 수행

---

## canopy 면적 계산

instance segmentation 결과로 각 나무의 `mask`가 나오면, canopy 면적은 픽셀 수를 이용해 바로 계산할 수 있다.

예시 공식:

```text
canopy_area = mask_pixel_count × (pixel_size_m)^2
```

예를 들어 해상도가 `0.2 m/pixel`이고 특정 mask가 `1000 pixel`이면:

```text
1000 × 0.2 × 0.2 = 40 m²
```

이 방식이 `object detection -> 중심점 추출 -> NDVI로 외곽 추정`보다 더 자연스럽고 정확하다.

---

## 직접 해야 하는 작업과 자동화 가능한 작업

### 직접 해야 하는 작업

- 사용할 항공영상 확보
- crown 경계 라벨 기준 결정
- 초기 라벨링 수행
- 결과 검수 및 수정

### 자동화 가능한 작업

- 타일 생성
- 학습 데이터셋 구성
- 모델 학습 및 추론 파이프라인
- canopy 면적 계산
- GIS 결과 파일 생성
- 시각화 및 통계 요약

즉, 사람이 가장 많이 개입하는 부분은 **초기 라벨링과 품질 검수**다.

---

## 예상 작업량

### 빠른 실험

- 라벨 수: `100-300`개 crown
- 목표: 방법이 작동하는지 확인
- 예상 시간: 반나절~하루

### 실용적인 초기 결과

- 라벨 수: `500-2000`개 crown
- 목표: 특정 지역에서 쓸만한 tree map 생성
- 예상 시간: `1-5일`

### 안정적인 운영 수준

- 수천 개 이상의 라벨
- 여러 지역, 여러 수종, 다양한 조명 조건 포함
- 반복적인 오류 수정 필요

---

## 추천 시작 전략

처음부터 대규모 라벨링을 하기보다, 아래 순서로 시작하는 것이 현실적이다.

1. 한 지역의 여름철 항공영상 확보
2. 나무 `200개` 내외를 직접 라벨링
3. `YOLO-seg` 또는 `Mask R-CNN`으로 1차 학습
4. 예측 결과를 보고 오탐/누락 유형 확인
5. 부족한 유형만 추가 라벨링
6. 위 과정을 `2-3회` 반복

이 방식은 초기 수작업 부담을 줄이면서도 빠르게 가능성을 확인할 수 있다.

---

## 기대 산출물

- 개별 나무 단위 `instance mask`
- tree centroid 또는 representative point
- tree count
- canopy area 통계
- GIS 호환 벡터 파일
- 시각화용 canopy map

---

## 한계와 주의사항

- crown이 매우 조밀하면 개별 분리가 어려울 수 있음
- 그림자, 건물 경계, 잔디와의 혼동 가능성이 있음
- 수종과 촬영 시기 차이에 따라 일반화 성능이 달라질 수 있음
- `RGB`만 있을 때보다 `NIR`가 있을 때 식생 판별이 더 안정적임

---

## 결론

개별 tree canopy 지도가 목표라면, `Landsat` 기반 접근보다 **고해상도 항공영상 기반 instance segmentation**이 적합하다.

`object detection`으로 위치만 찾고 `NDVI`로 canopy 모양을 추정하는 방식도 아이디어 차원에서는 가능하지만, 실제 구현과 정확도 측면에서는 **처음부터 crown mask를 직접 예측하는 접근**이 더 합리적이다.

따라서 본 프로젝트의 실질적 권장 방향은 다음과 같다.

- 데이터: 여름철 항공 정사영상
- 라벨: 개별 tree crown polygon
- 모델: `YOLO-seg` 또는 `Mask R-CNN`
- 보조 정보: `NDVI/NIR`
- 산출물: tree map, tree count, canopy area
